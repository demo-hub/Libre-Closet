name: Tag Release on Merge

on:
    pull_request:
        types: [closed]
        branches: [main]

jobs:
    tag:
        if: github.event.pull_request.merged == true && startsWith(github.event.pull_request.head.ref, 'release/')
        runs-on: ubuntu-latest
        permissions:
            contents: write
            actions: write
        steps:
            # Pinned: on a pull_request event checkout defaults to
            # refs/pull/N/merge, the synthetic test-merge commit, so the tag
            # would land on a commit that is not in main's history.
            - uses: actions/checkout@v4
              with:
                  ref: ${{ github.event.pull_request.merge_commit_sha }}
                  fetch-depth: 0

            - name: Extract version from branch name
              id: version
              run: |
                  BRANCH="${{ github.event.pull_request.head.ref }}"
                  VERSION="${BRANCH#release/}"
                  echo "tag=v${VERSION}" >> $GITHUB_OUTPUT

            - name: Create and push tag
              run: |
                  git tag ${{ steps.version.outputs.tag }}
                  git push origin ${{ steps.version.outputs.tag }}

            - name: Trigger Docker publish
              env:
                  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
              run: |
                  gh workflow run docker-publish.yml --ref ${{ steps.version.outputs.tag }}
