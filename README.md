# PR screenshots

Before and after images for pull requests, linked from their descriptions.
This branch has no shared history with `main` and is never merged; each
directory is named after the PR it belongs to. Delete a directory once its
PR has closed if the space matters.
